/**
 * Functional Modules Index
 * 
 * Exposes standalone, tree-shakeable functions (e.g. `parse`, `format`) that do not
 * require a full `Tempo` class instantiation, optimizing bundle size for simple use cases.
 */
export { parse } from './module.parse.js';
export { format } from './module.format.js';
