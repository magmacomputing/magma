# Business Utilities
This directory contains utility functions designed for financial, business, and SLA calculations.

## Exported Functions
- `workingHoursUntil`: Calculates the exact number of SLA-eligible working hours between a date and a deadline.
- `isSameFiscalQuarter`: Determines if a date falls within the same fiscal quarter as a target date.
